apiVersion: v1
kind: Service
metadata:
  name: auth-postgres
  namespace: video-processor-auth
  labels:
    app: auth-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: auth-postgres